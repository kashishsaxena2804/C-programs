#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    //strcpy
    char str1[5]="sia";
    char str2[10];
    strcpy(str2,str1);
   // printf(gets(str1));
    //printf(gets(str2));
    
    return 0; 
}