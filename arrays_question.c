/*
#include <stdio.h>
#include <stdlib.h>
int main(){
    int n,i,low;
    printf("enter the size:");
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        printf("enter the element:");
        scanf("%d",&arr[i]);
    }
    low=arr[0];
    for(i=0;i<n;i++){
        if(arr[i]<low){
            low=arr[i];
        }
    }
    printf("lowest num is %d",low);
    return 0;
}*/
/*
#include <stdio.h>
#include<stdlib.h>
int main(){
    int n,i,max;
    printf("enter the size");
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        printf("enter element:");
        scanf("%d",&arr[i]);
    }
    max=arr[0];
    for(i=0;i<n;i++){
        if(arr[i]>max){
            max=arr[i];
        }
    }
    printf("max is %d",max);
    return 0;
}
*/